name: neutron-imaging
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.7
  - jupyterlab
  - ipykernel
  - pip
  - pip:
    - -r file:../requirements.txt
prefix: /opt/anaconda/envs/neutron-imaging
